---
# - name: Configure remote server
#   hosts: nodo0
#   become: true
#   become_method: sudo
#   gather_facts: true
#   roles:
#     # - common
#     # - nvidia
#     # - proxmox
#     # - intel
#     - nodo0
#     # - finally

- name: Deploy Proxmox LXCs with Terraform via Ansible
  hosts: nodo0
  connection: local
  gather_facts: false
  vars_files:
    - host_vars/nodo0/vault.yml
  vars:
    proxmox_endpoint: "https://{{ ansible_host }}:8006/"
    pm_password: "{{ ansible_become_password }}"
  roles:
    - nodo0_create_tf_instances  # Optional: If structured as roles; otherwise use tasks below


...