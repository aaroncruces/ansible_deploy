- name: wgdashboard 
  include_tasks: "{{ playbook_dir }}/roles/portainer_deploys/tasks/generate_and_deploy_compose.yml"
  vars:
    compose_name: "wgdashboard"
    compose_extra_params: |
          # extra params
              networks:
                - nginxproxymanager
          networks:
            nginxproxymanager:
              external: true